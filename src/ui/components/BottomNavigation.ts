import { navigationLabels } from "../../app/Router";
import type { NavigationId } from "../../app/Store";
export const BottomNavigation = (active: NavigationId, onSelect: (id: NavigationId) => void): HTMLElement => { const nav = document.createElement("nav"); nav.className = "bottom-navigation"; nav.setAttribute("aria-label", "メインナビゲーション"); (Object.entries(navigationLabels) as [NavigationId, string][]).forEach(([id, label]) => { const button = document.createElement("button"); button.type = "button"; const mark = document.createElement("span"); mark.className = "nav-mark"; mark.setAttribute("aria-hidden", "true"); mark.textContent = id === "map" ? "⌂" : "·"; const text = document.createElement("span"); text.textContent = label; button.append(mark, text); if (id === active) { button.className = "is-active"; button.setAttribute("aria-current", "page"); } button.addEventListener("click", () => onSelect(id)); nav.append(button); }); return nav; };
