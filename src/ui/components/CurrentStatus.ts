import type { MapId } from "../../domain/maps/mapTypes";
export const CurrentStatus = (mapId: MapId): HTMLElement => { const section = document.createElement("section"); section.className = "content-card current-status"; const heading = document.createElement("h2"); heading.textContent = "現在の様子"; const status = document.createElement("p"); status.textContent = mapId === "starter_house_interior" ? "コーディは作業台で道具を整理しています。" : "コーディは家の中にいます。"; section.append(heading, status); return section; };
