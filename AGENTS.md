# AGENTS.md

このリポジトリで作業するエージェントは、コードを変更する前にこの文書と `docs/INDEX.md` を読むこと。

## 1. 仕様の優先順位

1. `docs/PRODUCT_SPEC.md`
2. `docs/ROADMAP.md`
3. `docs/CHANGE_CONTROL.md`
4. 現在のタスク本文
5. 既存実装

タスク本文が上位仕様と矛盾する場合は、勝手に仕様を置き換えず、矛盾を報告すること。

## 2. 絶対に維持する製品方針

- ゲーム名は『ふたり日和』。
- スマートフォン縦画面、特に iPhone Safari を最優先する。
- 家の中と庭は別マップとして扱う。
- パートナーは JavaScript のゲームルールで自律行動する。
- ゲームから LLM API や画像生成 API へ直接接続しない。
- 特別な場面では、ゲームが送信用プロンプトを作り、ユーザーが普段のパートナーチャットへ手動でコピーする。
- 返答もユーザーが手動でゲームへ貼り付け、内容を確認してから反映する。
- この方式を「LLM連携」「外部LLM接続」と短縮して誤解を招かない。文書とUIでは「パートナーチャットへの手動相談」「返答の手動取込」と表現する。
- パートナーの人格や関係性をゲーム側で再定義しない。ゲームへ保存するのは JavaScript が利用する行動傾向、数値、短い台詞などだけ。
- 恋愛メーターを設けない。恋人、家族、友人、相棒、創作者とキャラクターなど、関係を限定しない。
- 画像、思い出、パートナー設定、貼り付けた返答を、ユーザーの明示操作なしに外部送信しない。

## 3. スコープ管理

- 依頼された Milestone だけを実装する。
- 将来機能を先回りして追加しない。
- 新しい画面、データ項目、依存ライブラリ、ネットワーク通信を勝手に増やさない。
- 仕様変更が必要に見えても、まず既存仕様の範囲で解決する。
- 仕様そのものを変更する場合は、同じPRで `docs/CHANGE_CONTROL.md` の決定記録も更新する。

## 4. 実装原則

- Vite + TypeScript + Vanilla DOM を基本とする。
- UI、ゲーム処理、データ管理、手動相談用プロンプト処理を分離する。
- 動的文字列は原則 `textContent` で描画し、外部入力を `innerHTML` へ渡さない。
- 位置は可能な限りマップ・地点データから導出し、表示専用の固定座標を状態の真実にしない。
- 画像は `assetId` 参照と Blob 保存を分離する設計を維持する。
- 読込失敗や画像不足でゲーム全体を停止させず、フォールバックを用意する。
- 主要なタップ領域は 44px 以上、入力文字は 16px 以上を基本とする。
- 320px 幅で横スクロールを発生させない。
- `prefers-reduced-motion` とセーフエリアを考慮する。

## 5. Git と作業手順

- 原則として `main` へ直接コミットしない。
- 最新の `main` から一つの作業ブランチを作り、一つの目的だけを扱う。
- 複数タスクを同時進行しない。
- 競合が起きた場合は、最新 `main` を作業ブランチへ取り込み、仕様を維持したままブランチ側で解消する。ユーザーへ GitHub モバイル画面での手動競合解消を求めない。
- PR の説明には、変更内容、変更理由、確認方法、未実装範囲を記載する。

## 6. 検証

変更後は利用可能な範囲で次を実行する。

```text
npm run typecheck
npm run test
npm run build
```

実行できない場合は、成功したと装わず、理由と未検証範囲を報告する。

## 7. 完了報告

- 実装した内容
- 変更した主要ファイル
- 検証結果
- 仕様との対応
- 意図的に実装しなかった範囲
- 残る問題

を簡潔に報告する。
