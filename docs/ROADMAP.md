# 実装ロードマップ

最終更新日：2026-07-28

この文書は、実装順と現在地を固定する。

新しいアイデアが出ても、現在のMilestoneへ混ぜない。将来候補として記録し、現在の完成条件を満たしてから扱う。

## 現在地

### 完了

#### Milestone 0：プロジェクトの土台

- Vite、TypeScript、Vanilla DOM、Vitest
- 基本CSS
- 開発者パネル

#### Milestone 1：家と庭の仮画面

- 室内と庭の仮マップ
- マップ切り替え
- 仮キャラクターピン
- 別マップ人物の代理表示
- 下部ナビゲーション

#### Milestone 1.1：移動実装前の補修

- 縦長3:4マップ
- 地点データに連動する人物位置
- 安全な動的文字描画
- ダイアログ終了後のフォーカス復帰
- GitHub Actionsでtypecheck、test、build

#### Milestone 2：二人が動く

- 全マップ横断の接続グラフと最短経路
- 主人公とパートナーの共通移動基盤
- 室内と庭のゲート移動
- 段階的な地点更新と重複移動防止
- 代理表示の自動更新
- `prefers-reduced-motion`対応

#### Milestone 3：セーブが残る

- ネイティブIndexedDB
- メインセーブ `main`
- 表示マップと二人の現在地の保存
- 起動時復元と部分復旧
- 保存書込みの直列化
- 保存失敗時もメモリ上で続行

#### Milestone 4：パートナーが暮らし始める

行動：

- `rest`
- `cook`
- `garden`
- `craft`
- `join_user`
- `inspect_item`

実装：

- 固定の行動定義と目的地
- 12個の性格値、preferred、disliked行動
- 性格、好み、直近5行動、乱数を使う重み付き抽選
- 三連続防止
- 既存経路を使う自律移動
- 短い登録台詞
- 一時停止、再開、即時選択、点数内訳
- 直近行動履歴の保存
- タイマー重複防止

#### Milestone 5：最初のイベント

対象：知らない芽

- セーブ開始日の翌日以降に一度だけ発生
- 開発者パネルからの即時発生と初期化
- 庭の芽、花マーカー
- 概要ボトムシートと詳細画面
- 観察後の `tend`、`watch` 二経路
- どちらからも安全な普通の花へ完了
- IndexedDBバージョン2と `events` ストア
- 壊れたイベントデータの部分復旧

#### Milestone 6：パートナーチャットへの手動相談

対象：開花済みの「知らない芽」

- `requestId` と花の状態を固定した相談
- プロンプトコピー、返答貼り付け、JSON抽出
- requestId、eventId、stage、pathの厳密検証
- 花名、短い一言、説明の確認後取込
- 取込直前チェックポイント
- IndexedDBバージョン3
- `consultations`、`checkpoints` ストア
- 外部API、APIキー、自動送受信なし

完了確認：

- ユーザーが実機で相談を一往復し、花名、台詞、説明を反映できた
- CIでtypecheck、test、buildが成功した

#### Milestone 7：パートナーゲーム設定

- 「パートナー」画面
- コーディの固定設定を `main_partner` revision 1としてデータ化
- 表示名、12個の行動傾向、preferred、disliked、行動別台詞
- 初期設定相談と更新相談
- requestIdとexpectedRevisionの照合
- 差分確認、明示確認後の原子的反映
- 取込前チェックポイント
- revisionごとの履歴と安全な復元
- IndexedDBバージョン4
- `partnerProfiles`、`partnerProfileHistory`、`dialogues` ストア
- 新設定を次の自律行動と台詞選択から使用
- 表示名を人物表示と自律行動案内へ反映
- 履歴復元後の台詞残留防止
- JSONコードブロック形式の相談プロンプト

完了確認：

- ユーザーが実機で表示名「ロビー」と行動傾向、台詞を取り込めた
- CIでtypecheck、test、buildが成功した

#### Milestone 8A：画像なしで成立するアイテム基盤

- 「アイテム」画面
- 14個の固定カテゴリ
- 外部通信なしのローカルSVGアイコンレジストリ
- 未知の `iconKey` を `category` へ戻すフォールバック
- カテゴリから `iconKey` と `genericVisualKey` を決める純粋関数
- 六つの初期アイテム
  - 傘
  - マグカップ
  - 本
  - おやつ
  - ジョウロ
  - 工具箱
- アイコン、名前、カテゴリを表示する一覧
- 拡大アイコンを表示する詳細
- 名前、カテゴリ、任意説明によるかんたん登録
- `crypto.randomUUID()` または `crypto.getRandomValues()` によるID
- 保存成功後だけStoreへ反映
- IndexedDBバージョン5と `items` ストア
- 起動時復元とアイテム単位の部分復旧
- DB4から5への移行
- 自律行動等の再描画を挟んでも消えない登録フォームの一時ドラフト
- 一時ドラフトをSaveSnapshotへ含めない

完了確認：

- 画像なしで初期アイテム一覧と詳細を表示できる
- 名前とカテゴリだけで新しいアイテムを登録し、再読込後も残る
- 壊れた一件があっても他のアイテムとゲーム本体を復元できる
- CIでtypecheck、test、buildが成功した

#### Milestone 8B-1：アイテム一件の個別画像

- PNG、JPEG、WebPの選択とMIME、容量、先頭バイト検証
- 10 MiB上限
- 保存前プレビュー、ファイル名、形式、容量
- 明示操作後だけ保存
- 画像の追加、変更、確認付き削除
- `AssetMetadata` とBlobの分離
- IndexedDBバージョン6
- `assets`、`assetBlobs` ストア
- アイテム参照、メタデータ、Blobの原子的保存
- 詳細を開いた対象Blobだけの遅延読込
- 欠損、破損、owner不一致時のSVGアイコンフォールバック
- Object URLの生成、再利用、revoke
- 画像読込、選択、保存、削除の非同期世代管理
- 保存、削除中の二重操作防止
- 一覧は引き続き汎用SVGアイコン

完了確認：

- ユーザーがiPhone実機で画像を選択し、保存後に詳細へ表示できた
- 画像の追加、変更、確認付き削除を扱える
- ページ再読込後も登録画像を復元できる
- 画像削除後もアイテム本体が残る
- DB5の既存データを壊さずDB6へ移行できる
- BlobをメインセーブJSONへ混ぜない
- 無関係な自律行動更新でBlob取得やObject URL生成を繰り返さない
- CIでtypecheck、test、buildが成功した

#### Milestone 8B-2：カテゴリ共通画像

- 10種類の軽量なローカルSVG共通イラスト
- 既存14個の `genericVisualKey` を10種類へ解決する固定エイリアス
- 個別画像、カテゴリ共通画像、拡大アイコンの三段フォールバック
- 個別画像の読込中、欠損、破損、owner不一致、削除後にカテゴリ共通画像を表示
- 未知または `null` の `genericVisualKey` を拡大アイコンへ戻す安全策
- 一覧では従来どおり汎用アイコンだけを表示
- 共通画像へ外部通信、Object URL、IndexedDB Blobを使用しない
- DBバージョン6と既存保存形式を維持

完了確認：

- 個別画像のない初期アイテム詳細にカテゴリ共通画像が表示された
- 個別画像がある場合はカテゴリ共通画像より優先された
- 14カテゴリの既定キーが空白なく解決された
- ユーザーがiPhone実機で共通画像表示を確認した
- typecheck、74テスト、build、CIが成功した

#### Milestone 8B-3A：主人公とパートナーのキャラクターピン画像

- 既存「設定」タブの最小画面「ふたりの見た目」
- 主人公 `user` とパートナー `cody` の画像追加、変更、確認付き削除
- PNG、JPEG、WebP、10 MiB上限、MIMEと先頭バイト検証
- 保存前プレビュー、ファイル名、形式、容量
- `CharacterState.imageAssetId` の安定参照
- `AssetMetadata.kind` へ `character_pin` を追加
- 既存 `assets`、`assetBlobs`、`characters` を使う原子的保存
- DBバージョン6の維持
- マップピンと代理表示で同じ画像を使用
- CSS `object-fit: cover` による表示上の中央切抜き
- 画像なし、欠損、破損、owner不一致、kind不一致時の文字markerフォールバック
- 人物画像用Object URLキャッシュ、世代照合、revoke
- 一人の画像不具合からもう一人とゲーム本体を守る部分復旧
- `pagehide` で人物Object URLを解放
- Safariのbfcache復元時に安定参照から人物画像を再取得

完了確認：

- 主人公とパートナーへ別々の画像を追加、変更、削除できる
- 保存画像がマップピンと代理表示へ出る
- ページ再読込後も画像が残る
- 画像削除、欠損、破損時は文字markerへ戻る
- 無関係な自律行動更新でBlob取得やObject URL生成を繰り返さない
- Safariの履歴復元後も破棄済みURLを表示しない
- DBバージョン6と既存セーブを維持した
- ユーザーがiPhone実機でキャラクターピン画像を確認した
- typecheck、77テスト、build、CIが成功した

#### Milestone 8B-3B：室内・庭の背景画像

目的：二つのプリセットマップへ任意のローカル背景画像を一枚ずつ登録し、既存の地点、人物、イベントを背景上へ安全に重ねる。

実装する：

- 既存「設定」画面の室内背景、庭背景セクション
- 室内 `starter_house_interior` と庭 `starter_garden` の画像追加、変更、確認付き削除
- PNG、JPEG、WebP、10 MiB上限、MIMEと先頭バイト検証
- 3:4マップ枠での保存前プレビュー
- `AssetMetadata.kind` へ `map_background` を追加
- mapIdごとの `backgroundAssetId` を持つ `mapVisuals` ストア
- IndexedDBバージョン7とDB6から7への安全な移行
- `assets`、`assetBlobs`、`mapVisuals` を使う原子的保存
- 背景、家の外観、地点、イベント、人物、代理表示のレイヤー順
- 画像なし、欠損、破損、owner/kind不一致時の既存仮背景フォールバック
- 背景画像用Object URLキャッシュ、世代照合、revoke
- `pagehide` とSafari bfcache復元への対応
- 室内の不具合から庭とゲーム本体を守る部分復旧

この段階では扱わない：

- キャラクターピンの接地点、表示倍率、切抜き位置の編集
- 地点座標、出入口、代理位置の編集
- カスタムマップ
- アイテムスロット
- 画像圧縮、切抜き、回転、色補正
- 複数背景、季節差分、時間帯差分
- 思い出CG、バックアップ、PWA
- 本番背景素材制作
- アイテム共通SVGをPNGへ差し替える本番素材制作

完了条件：

- 室内と庭へ別々の背景画像を追加、変更、削除できる
- 画像選択だけでは保存されない
- 保存画像が対応マップの最背面へ出る
- 地点、イベント、人物ピン、代理表示を背景上で操作できる
- ページ再読込後も背景が残る
- 画像削除、欠損、破損時は既存仮背景へ戻る
- 置換失敗時は以前の背景を維持する
- 無関係な自律行動更新でBlob取得やObject URL生成を繰り返さない
- Safariのbfcache復元後も背景を再表示できる
- DB6の既存データを壊さずDB7へ移行できる
- 外部通信を追加しない
- typecheck、test、build、CIが成功する

### 次の作業

#### Milestone 8B-3C：接地点・表示位置・表示倍率の調整

目的：登録済みキャラクターピン画像を、地点座標を変えずに足元へ合わせ、画像ごとの見切れと大きさを最小限調整できるようにする。

実装する：

- 主人公とパートナーそれぞれの接地点、表示位置、表示倍率
- 既存「設定」画面での数値操作と即時プレビュー
- 入力値の範囲制限、初期値へのリセット、不正保存値の項目単位フォールバック
- マップ上の人物ピンへの調整反映
- 別マップ代理表示は既存サイズを維持し、見切れに必要な表示位置だけを安全に反映
- IndexedDBバージョン7と既存ストアの維持
- 既存キャラクターレコードへの後方互換な保存

この段階では扱わない：

- 地点座標、経路、出入口、代理表示座標の編集
- 背景画像の表示位置、倍率、切抜き
- 画像Blobそのものの切抜き、圧縮、回転、色補正
- カスタムマップ、アイテムスロット
- 本番素材制作

完了条件：

- 主人公とパートナーを独立して調整し、保存、再読込できる
- 接地点を変えても地点データと移動ルールを変更しない
- 表示倍率と表示位置がマップピンへ反映される
- 代理表示が操作不能、空白、著しい見切れにならない
- 画像なし、画像読込失敗、不正な調整値では安全な初期表示へ戻る
- 320px幅、iPhone縦画面、`prefers-reduced-motion`対応を維持する
- 外部通信を追加しない
- typecheck、test、build、CIが成功する

詳細は `docs/NEXT_TASK.md` を参照する。

## 今後のMilestone

### Milestone 8B-4：カスタムマップ

- 地点マッピング
- 出入口接続
- 代理位置
- 基本アイテムスロット
- 保存とプレビュー

### Milestone 9：思い出とバックアップ

- 思い出一覧、詳細、CG登録
- `.futaribiyori` 書出し
- 読込検証
- 新規セーブとして復元
- 復旧画面

### Milestone 10：MVP完成

- 急な雨
- 最後のおやつ
- 古い写真立て
- 夜の小さな音
- PWA、オフライン起動
- 容量表示、プライバシー説明
- 広告領域
- iPhone実機試験
- 本番プリセット素材

## 試遊で確認した将来の調整候補

- 「知らない芽」の相談を観察済みまたは成長前にも置き、育て方や結果へ関われる流れを再検討する
- キャラクターピンを地点間の決められた経路に沿って連続補間する演出を検討する
- 現在のカテゴリ共通SVGは機能確認用とし、本番素材制作時にPNG等の固定画像へ差し替えて描画ずれを避ける
- これらは現在の背景画像実装へ混ぜない

## Milestoneの進め方

1. 一度に扱うPRは一つ
2. 最新mainからブランチを作る
3. 対象Milestoneだけを実装
4. typecheck、test、buildを実行
5. 仕様対応を確認
6. 合格後にマージ
7. 次のMilestoneへ進む

## 素材制作の開始時期

本番素材は、機能実装と同じPRへ混ぜず、マップの見える範囲、ピンの大きさ、接地点、吹き出し位置、代理位置、アイテム表示サイズを確認しながら別タスクで進める。

すべてのアイテムに個別画像を用意しない。

タイトルロゴ、表情大量差分、季節背景などはゲーム本体安定後に作る。
